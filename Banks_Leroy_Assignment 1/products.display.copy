<html>

<head>
    <title>Leroy Banks Assignment1</title>
</head>

<body>
    <h1>Black Friday Sales For Jordans</h1>
    <div>
        <section class="item" ;>
            <h2>Jordan 8 Retro</h2>
            <p>&dollar;134.97</p>
            <img src="./images/Jordan_8_Retro.jpg"></img>
        </section>
        <section class="item" ;>
            <h2>Jordan 9 Retro</h2>
            <p>&dollar;140.00</p>
            <img src="./images/Jordan_9_Retro.jpg"></img>
        </section>
        <section class="item" ;>
            <h2>Jordan 10 Retro</h2>
            <p>&dollar;140.00</p>
            <img src="./images/Jordan_10_Retro.jpg"></img>
        </section>
        <section class="item" ;>
            <h2>Jordan 11 Retro</h2>
            <p>&dollar;285.00</p>
            <img src="./images/Jordan_11_Retro.jpg"></img>
        </section>
        <section class="item" ;>
            <h2>Jordan 12 Retro</h2>
            <p>&dollar;140.00</p>
            <img src="./images/Jordan_12_Retro.jpg"></img>
        </section>

    </div>

    <script>
        function isNonNegInt(q, sendArrayBack = false) {
            errors = []; // assume no errors at first
            if (Number(q) != q) errors.push('Not a number!'); // Check if string is a number value
            if (q < 0) errors.push('Negative value!'); // Check if it is non-negative
            if (parseInt(q) != q) errors.push('Not an integer!'); // Check that it is an integer

            if (sendArrayBack) {
                return (errors);
            } else {
                return (errors.length == 0);
            }
        }

        function checkQuantity() // setting the function for the quantity textbox
        {
            errs = isNonNegInt(quantity_textbox.value, true);
            qty_textbox_message.innerHTML = errs.join(",");
            //quantity_textbox_message.innerHTML = quantity_textbox.value;
        }

        function displayPurchase() {
            // function to display product quantity if it is a non-negative integer 
            textboxValue = quantity_textbox.value;
            if (isNonNegInt(textboxValue)) {
                document.write(`Thank you for purchasing ${textboxValue} things`);
            }
        }
    </script>

    <label for="quantity_textbox">Enter quantity desired :</label>
    <input type="text" id="quantity_textbox" onkeyup="checkQuantity();">
    <br><span id='qty_textbox_message'>Enter a quantity</span>
    <input type="button" value="Purchase" onlclick="displayPurchase();">
</body>

</html>